# GAIA Docker Environment Configuration
# Copy this file to .env and fill in your values
# The .env file is gitignored and won't be committed

# Optional: Lemonade server URL (defaults to http://localhost:5000/api/v1)
# Examples:
#   - Default (Lemonade in container): http://localhost:5000/api/v1
#   - Remote server: https://your-server.com/api/v1
#   - Local server on host (macOS): http://host.docker.internal:5000/api/v1
#   - Local server on host (Linux): http://172.17.0.1:5000/api/v1
# LEMONADE_URL=http://localhost:5000/api/v1

# Optional: GAIA repository configuration
# GAIA_BRANCH=main
# GAIA_REPO=https://github.com/amd/gaia.git

# Optional: Container name (default: gaia-dev)
# CONTAINER_NAME=gaia-dev

# Optional: Port mappings (defaults shown)
# LEMONADE_PORT=5000
# GAIA_API_PORT=8000
# WEB_PORT=3000

# Optional: Skip pip install on restart (for faster iteration)
# SKIP_INSTALL=false

# Optional: Mount host directory at /host in container
# HOST_DIR=/path/to/your/directory
